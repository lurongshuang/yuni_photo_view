name: yuni_photo_view_example
description: Demonstrates how to use the yuni_photo_view plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  palette_generator: ^0.3.3+7
  video_player: ^2.9.2
  yuni_photo_view:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
